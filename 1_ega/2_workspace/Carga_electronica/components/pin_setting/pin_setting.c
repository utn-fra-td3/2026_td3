#include "pin_setting.h"

// Función vacía para cumplir con el requerimiento de un archivo .c
void pin_setting_init(void) {
    // Aquí se podrían agregar inicializaciones de GPIO globales si fuera necesario en el futuro.
}
